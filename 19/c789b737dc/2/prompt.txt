Task: You are a delegated subagent running from a fork of the parent session. Treat the inherited conversation as reference-only context, not a live thread to continue. Do not continue or answer prior messages as if they are waiting for a reply. Your sole job is to execute the task below and return a focused result for that task using your tools.

Task:
Address the review findings in skills/README.md in /Users/sofianebebert/workspace/sof-pi. Edit only skills/README.md; do not commit.

Add concise, accurate guidance covering:
- Package discovery: package.json's ./skills root, the directory-based SKILL.md convention, and any supported top-level markdown skill form only if confirmed by inspecting package.json/repo. State frontmatter expectations: name and description required for packaged directory skills, compatibility/metadata optional if that matches the repo.
- Helper scripts: skill-local scripts are support assets, paths resolve from the skill directory, document dependencies/invocation, and prefer them over duplicated logic when appropriate. Mention actual repo examples only if they exist.
- Root README synchronization: package-root README.md must be updated whenever a skill is added, removed, or meaningfully changed; skills table text should stay aligned with the skill frontmatter description per AGENTS.md.
- Cross-skill ownership: identify a canonical skill for shared mechanics, link/delegate to it, and avoid duplicated/conflicting instructions.
- Versioning: say increment metadata version when the skill has version metadata, or establish a clearly qualified convention rather than universalizing it.
- Preserve the existing six principles, safety guidance, observed-evidence update loop, no arbitrary line limit, and no blanket ban on precise safety-critical procedures.

Inspect package.json, AGENTS.md, root README.md, and relevant helper files before editing. Run git diff --check and validate that referenced paths exist. Report changed file, validation, and any uncertainty. Do not alter other files or commit.

## Acceptance Contract
Acceptance level: checked
Completion is not accepted from prose alone. End with a structured acceptance report.

Criteria:
- criterion-1: Implement the requested change without widening scope

Required evidence: changed-files, tests-added, commands-run, residual-risks, no-staged-files, validation-output, diff-summary

Finish with a fenced JSON block tagged `acceptance-report` in this shape:
Use empty arrays when no items apply; array fields contain strings unless object entries are shown.
Empty-string entries (`[""]`) are ignored; use `[]` when nothing applies.
`criteriaSatisfied[].status` must be exactly one of: satisfied, not-satisfied, not-applicable.
`commandsRun[].result` must be exactly one of: passed, failed, not-run.
`manualNotes` and `notes` are optional strings; an empty string means no note and does not satisfy `manual-notes` evidence.
```acceptance-report
{
  "criteriaSatisfied": [
    {
      "id": "criterion-1",
      "status": "satisfied",
      "evidence": "specific proof"
    }
  ],
  "changedFiles": [
    "src/file.ts"
  ],
  "testsAddedOrUpdated": [
    "test/file.test.ts"
  ],
  "commandsRun": [
    {
      "command": "command",
      "result": "passed",
      "summary": "short result"
    }
  ],
  "validationOutput": [
    "validation output or concise summary"
  ],
  "residualRisks": [
    "none"
  ],
  "noStagedFiles": true,
  "diffSummary": "short description of the diff",
  "reviewFindings": [
    "blocker: file.ts:12 - issue found, or no blockers"
  ],
  "manualNotes": "anything else the parent should know"
}
```